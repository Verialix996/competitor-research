# Legacy Scoring Archive

These JavaScript rankers are retained only for audit of the pre-Phase-0
methodology. They used deprecated score columns, fixed category/company logic,
and numeric defaults for missing evidence.

They are not referenced by generated HTML and must not be restored to the
active website or used for product decisions.

The current scoring policy is documented in `SCORING_METHODOLOGY.md`.
